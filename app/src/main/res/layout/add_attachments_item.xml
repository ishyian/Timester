<?xml version="1.0" encoding="utf-8"?>
<TextView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/text_add_recipient"
    android:layout_width="@dimen/offset50"
    android:layout_height="@dimen/offset50"
    android:layout_marginHorizontal="@dimen/offset7"
    android:background="@drawable/bg_add_attachment"
    android:fontFamily="@font/open_sans_semibold"
    android:gravity="center"
    android:includeFontPadding="false"
    android:text="+"
    android:textColor="@color/attachment_text_color"
    android:textSize="@dimen/text18"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintTop_toTopOf="parent" />